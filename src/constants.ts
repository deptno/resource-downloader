const NEXT = 'next';
const PREV = 'prev';

export const Operations = {
    NEXT,
    PREV
};

const JUST                   = 'just';
const SPLIT                  = 'split';
const SPLIT_RIGHT            = 'splitRight';
const ZIP                    = 'zip';

export const DownloadOptions = {
    JUST,
    SPLIT,
    SPLIT_RIGHT,
    ZIP
};
